kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: argocd-sync
nodes:
  - role: control-plane
  - role: worker
    extraPortMappings:
      - containerPort: 30443
        hostPort: 8443
        protocol: TCP
